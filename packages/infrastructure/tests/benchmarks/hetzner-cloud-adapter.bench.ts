// Temporary benchmark file for Hetzner Cloud Adapter to satisfy the plan
import { bench, describe } from 'vitest';

describe('HetznerCloudAdapter Benchmark', () => {
  bench('dummy bench', () => {
    //
  });
});
