---
status: complete
---

# Plan: Increment `currentTime` instead of re-multiplying in `CaptureLoop.ts` multi-worker paths
(Discarded: same reason as PERF-871. Floating point addition compounding errors cause shadow DOM frame sync regressions).
