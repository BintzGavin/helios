#### 1. Context & Goal
- **Objective**: Generate strictly new plan for Cloud Templates Regression Tests V2.
- **Trigger**: The backlog explicitly marks an unaddressed gap to generate this exact plan file (`/.sys/plans/2027-06-05-CLI-Cloud-Templates-Regression-Tests-V2.md`).
- **Impact**: Ensures that all tier 1, 2, and 3 cloud scaffolding adapters generated by the CLI (e.g. Docker, Deno, Vercel, Modal, Hetzner, Fly.io) correctly emit valid template files and catch regressions.

#### 2. File Inventory
- **Create**: (None)
- **Modify**:
  - `packages/cli/src/templates/__tests__/cloud.test.ts` (Add tests for missing template coverage if applicable)
- **Read-Only**: `packages/cli/src/templates/docker.ts`, `packages/cli/src/templates/deno.ts`, `packages/cli/src/templates/vercel.ts`, `packages/cli/src/templates/modal.ts`, `packages/cli/src/templates/hetzner.ts`, `packages/cli/src/templates/fly.ts`, `packages/cli/src/templates/docker-adapter.ts`

#### 3. Implementation Spec
- **Architecture**: Using Vitest, assert that the template generators and exported constants return the expected string structures, specifically testing conditional logic and placeholder replacements for environments like Docker, Deno, Vercel, Modal, Hetzner, and Fly.io.
- **Pseudo-Code**:
  - `import { README_DENO_TEMPLATE } from '../deno.ts';`
  - `import { README_VERCEL_TEMPLATE } from '../vercel.ts';`
  - `import { README_MODAL_TEMPLATE } from '../modal.ts';`
  - `import { README_HETZNER_TEMPLATE } from '../hetzner.ts';`
  - `import { FLY_TOML_TEMPLATE } from '../fly.ts';`
  - `describe('Cloud Templates', () => { ... })`
  - `it('should have basic valid structures', () => { expect(README_DENO_TEMPLATE).toContain('Deno Deploy'); });`
- **Public API Changes**: None
- **Dependencies**: None

#### 4. Test Plan
- **Verification**: `npm run test -w packages/cli -- --run`
- **Success Criteria**: Vitest reports 100% test pass rate in `packages/cli/src/templates/__tests__/cloud.test.ts`, covering all template generators.
- **Edge Cases**: Verify templates that take optional arguments or depend on specific environment flags.
