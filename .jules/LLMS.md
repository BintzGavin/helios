## v1.29.0 - Infrastructure Package Integration
**Learning**: The `@helios-project/infrastructure` package manages distributed rendering orchestration and cloud execution adapters.
**Action**: Included the `infrastructure` package in `llms.txt` under `Quick Facts`, `Packages`, and `Roadmap` to ensure agents are aware of its capabilities and the ongoing progress.
