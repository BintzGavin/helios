## 1.86.1 - Upgrading Misleading Examples
**Learning:** Found that `podcast-visualizer` was using fake CSS animations despite claiming to show "waveforms".
**Action:** Prioritized upgrading misleading examples over documenting hidden ones (`excalidraw-animation`), as correctness is more important than coverage.
